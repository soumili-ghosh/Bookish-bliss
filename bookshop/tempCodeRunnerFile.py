button_positions = [(220, 380), (500, 380), (820, 380)]
    # button_texts = ["Add Book", "Show Book", "Sell Book"]
    # button_commands = [add_book, show_book, sell_book]

    # for pos, text, command in zip(button_positions, button_texts, button_commands):
    #     button = AnimatedButton(root, text=text, font=("Helvetica", 18), command=add_book,command=sell_book)
    #     canvas.create_window(pos, window=button)